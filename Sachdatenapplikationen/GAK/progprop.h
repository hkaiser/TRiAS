// Program Properties 
// verwalten der persistenten Einstellungen des Programmes
// Autor JG

/*
class CProgProp
{
	// wrapping the GetProfileSections
	inline CString GetProperty(const CString& section, const CString& entry, CString& defval)
	{
		AfxGetApp()->GetProfileString(section, entry, default);
	}
	// wrapping the 
	inline long GetProperty(const CString& section, const CString& entry, long& setting);
	{
		AfxGetApp()->GetProfileString(section, entry, default);
	}
};
*/
